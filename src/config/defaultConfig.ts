import { FullConfig } from '../types';
import { defaultStartupCheats, defaultCheatConfig } from './cheatConfig';
import { defaultInjectorConfig } from './injectorConfig';

export const defaultConfig: FullConfig = {
  startupCheats: defaultStartupCheats,
  cheatConfig: defaultCheatConfig,
  injectorConfig: defaultInjectorConfig,
};
